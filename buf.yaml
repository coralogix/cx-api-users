version: v1

deps:
- buf.build/googleapis/googleapis
lint:
  use:
    - DEFAULT
  ignore_only:
    PACKAGE_VERSION_SUFFIX:
      - com/coralogix/user/settings/audit_log_description.proto
breaking:
  use:
    - WIRE
